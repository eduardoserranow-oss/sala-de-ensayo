const GATEWAY_URL="https://ai-gateway.vercel.sh/v1/chat/completions";
const MODEL="openai/gpt-5.6-sol";
const INSTRUMENTS=["Guitarra eléctrica","Bajo eléctrico","Guitarra acústica","Voz","Piano / Keys","Otro"];
const LEVELS=["Sin repertorio","Repertorio en desarrollo","Repertorio establecido"];
const JOURNEYS=["Aprender fundamentos primero","Ruta más rápida al estilo final","Llenar vacíos musicales","Construir repertorio profesional"];
function sendJson(r,s,b){r.statusCode=s;r.setHeader("Content-Type","application/json; charset=utf-8");r.setHeader("Cache-Control","no-store");r.end(JSON.stringify(b))}
function text(v,m){return typeof v==="string"?v.trim().slice(0,m):""}function clean(v,m=320){return text(v,m).replace(/[<>]/g,"")}function parseBody(q){if(typeof q.body==="string")return JSON.parse(q.body||"{}");return q.body||{}}function fail(m,s=400){const e=new Error(m);e.status=s;throw e}
function schema(n){const song={type:"object",additionalProperties:false,properties:{artist:{type:"string"},title:{type:"string"},study:{type:"string"}},required:["artist","title","study"]};return{type:"object",additionalProperties:false,properties:{name:{type:"string"},objective:{type:"string"},stages:{type:"array",minItems:n,maxItems:n,items:{type:"object",additionalProperties:false,properties:{title:{type:"string"},objective:{type:"string"},a:song,b:song,connection:{type:"string"}},required:["title","objective","a","b","connection"]}}},required:["name","objective","stages"]}}
function normalizeProject(v,n){if(!v||typeof v!=="object"||!Array.isArray(v.stages)||v.stages.length!==n)fail("La IA devolvió una ruta incompleta. Inténtalo de nuevo.",502);const stages=v.stages.map((s,i)=>{const x={id:`ai-stage-${Date.now()}-${i+1}`,title:clean(s?.title,90),objective:clean(s?.objective,280),a:{artist:clean(s?.a?.artist,90),title:clean(s?.a?.title,100),study:clean(s?.a?.study,300)},b:{artist:clean(s?.b?.artist,90),title:clean(s?.b?.title,100),study:clean(s?.b?.study,300)},connection:clean(s?.connection,360)};if(!x.title||!x.objective||!x.a.artist||!x.a.title||!x.a.study||!x.b.artist||!x.b.title||!x.b.study||!x.connection)fail("La IA devolvió una etapa incompleta. Inténtalo de nuevo.",502);return x});const project={name:clean(v.name,70),objective:clean(v.objective,280),stages};if(!project.name||!project.objective)fail("La IA no pudo nombrar la ruta. Inténtalo de nuevo.",502);return project}
export default async function handler(request,response){if(request.method!=="POST"){response.setHeader("Allow","POST");return sendJson(response,405,{ok:false,error:"Método no permitido."})}try{const body=parseBody(request);const instrument=text(body.instrument,60),level=text(body.level,60),destination=clean(body.destination,120),journey=text(body.journey,80),knownSongs=clean(body.knownSongs,1200),stageCount=Number(body.stageCount);if(!INSTRUMENTS.includes(instrument))fail("Selecciona un instrumento válido.");if(!LEVELS.includes(level))fail("Selecciona tu nivel de repertorio.");if(!destination||destination.length<3)fail("Escribe el estilo o destino al que quieres llegar.");if(!JOURNEYS.includes(journey))fail("Selecciona el tipo de recorrido.");if(![6,8,12].includes(stageCount))fail("Selecciona 6, 8 o 12 etapas.");const token=process.env.AI_GATEWAY_API_KEY||process.env.VERCEL_OIDC_TOKEN;if(!token)fail("La conexión de IA todavía no está disponible.",503);const system=`Eres el diseñador pedagógico de Study Paths de FORTISSIMO. Creas currículos de repertorio musical, no rutinas técnicas ni reproductores. Responde en español. Cada etapa contiene una dupla: Canción A enseña el lenguaje, su raíz histórica o fundamento; Canción B aplica, evoluciona o reconoce ese lenguaje en una obra más moderna o cercana al destino. Usa únicamente canciones reales y verificables con partes relevantes para el instrumento indicado. No inventes títulos, artistas ni colaboraciones. Diseña una progresión coherente hacia el destino, explica qué se estudia en cada canción y la conexión A → B. El nivel describe repertorio, no destreza técnica. Evita repetir canciones. Si el usuario aporta canciones que ya conoce, NO las incluyas: sustituye cada una por repertorio diferente que conserve o mejore la misma función pedagógica. No incluyas tabs, stems, enlaces, metrónomo ni práctica dentro de FORTISSIMO.`;const user=`Instrumento: ${instrument}\nNivel de repertorio: ${level}\nDestino musical: ${destination}\nTipo de recorrido: ${journey}\nCantidad exacta de etapas: ${stageCount}\nCanciones que ya conoce y deben evitarse: ${knownSongs||"Ninguna indicada"}\n\nGenera nombre, objetivo general y exactamente ${stageCount} etapas. Mantén cada explicación clara y breve.`;const gateway=await fetch(GATEWAY_URL,{method:"POST",headers:{Authorization:`Bearer ${token}`,"Content-Type":"application/json"},body:JSON.stringify({model:MODEL,stream:false,messages:[{role:"system",content:system},{role:"user",content:user}],response_format:{type:"json_schema",json_schema:{name:"fortissimo_study_path",description:"Ruta musical progresiva con parejas de canciones",strict:true,schema:schema(stageCount)}}}),signal:AbortSignal.timeout(55000)});const payload=await gateway.json().catch(()=>null);if(!gateway.ok){console.error("[study-path-ai] gateway",gateway.status,payload?.error?.message||"unknown");if(gateway.status===429)fail("La IA está ocupada ahora mismo. Espera un momento e inténtalo otra vez.",429);fail("No pudimos generar la ruta ahora mismo. Inténtalo otra vez.",502)}const content=payload?.choices?.[0]?.message?.content;if(!content)fail("La IA no devolvió una ruta. Inténtalo otra vez.",502);return sendJson(response,200,{ok:true,project:normalizeProject(JSON.parse(content),stageCount)})}catch(error){if(error?.name==="TimeoutError")return sendJson(response,504,{ok:false,error:"La ruta tardó demasiado. Inténtalo otra vez."});if(error instanceof SyntaxError)return sendJson(response,502,{ok:false,error:"La IA devolvió una respuesta que no pudimos leer. Inténtalo otra vez."});console.error("[study-path-ai] generation failed",error?.message||error);return sendJson(response,error?.status||500,{ok:false,error:error?.status?error.message:"No pudimos generar la ruta ahora mismo."})}}