name: FORTISSIMO Desktop Shell CI

on:
  push:
    branches:
      - main
      - 'feature/fortissimo-desktop-*'
    paths:
      - 'desktop/**'
      - 'assets/login-auth.js'
      - 'assets/fortissimo-cloud-v1.js'
      - 'assets/vibe-roulette-desktop-midi-drag-v1.js'
      - 'assets/vibe-roulette-desktop-workspace-v1.js'
      - 'assets/vibe-roulette-spin-audio-sync-v1.js'
      - 'assets/vibe-roulette-wide-layout-v1.css'
      - 'assets/vibe-roulette-songstarter-export-v1.js'
      - 'assets/favicon.png'
      - 'assets/forte-favicon.ico'
      - 'assets/fortissimo-header-logo-v6.jpg'
      - 'assets/fortissimo-icon-20260824.svg'
      - '.github/workflows/fortissimo-desktop-shell-ci.yml'
      - '.github/workflows/fortissimo-desktop-release.yml'
  pull_request:
    paths:
      - 'desktop/**'
      - 'assets/login-auth.js'
      - 'assets/fortissimo-cloud-v1.js'
      - 'assets/vibe-roulette-desktop-midi-drag-v1.js'
      - 'assets/vibe-roulette-desktop-workspace-v1.js'
      - 'assets/vibe-roulette-spin-audio-sync-v1.js'
      - 'assets/vibe-roulette-wide-layout-v1.css'
      - 'assets/vibe-roulette-songstarter-export-v1.js'
      - 'assets/favicon.png'
      - 'assets/forte-favicon.ico'
      - 'assets/fortissimo-header-logo-v6.jpg'
      - 'assets/fortissimo-icon-20260824.svg'
      - '.github/workflows/fortissimo-desktop-shell-ci.yml'
      - '.github/workflows/fortissimo-desktop-release.yml'

jobs:
  shell-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Desktop Phase 7 syntax, security, MIDI toolkit, updater, adaptive layout and native boot contracts
        working-directory: desktop
        run: npm test

  windows-installer:
    if: github.event_name == 'push'
    needs: shell-contract
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Install Desktop build dependencies
        working-directory: desktop
        run: npm install --no-audit --no-fund
      - name: Build unsigned FORTISSIMO Windows installer
        working-directory: desktop
        run: npm run make
      - name: Verify Squirrel.Windows release assets
        shell: pwsh
        run: |
          $installer = Get-ChildItem -Path "desktop/out/make" -Recurse -Filter "FORTISSIMO-Setup.exe" | Select-Object -First 1
          $nupkg = Get-ChildItem -Path "desktop/out/make" -Recurse -Filter "*-full.nupkg" | Select-Object -First 1
          $releases = Get-ChildItem -Path "desktop/out/make" -Recurse -Filter "RELEASES" | Select-Object -First 1
          if (-not $installer) { throw "FORTISSIMO-Setup.exe was not produced." }
          if (-not $nupkg) { throw "Squirrel full .nupkg was not produced." }
          if (-not $releases) { throw "Squirrel RELEASES manifest was not produced." }
          Write-Host "Installer: $($installer.FullName)"
          Write-Host "Update package: $($nupkg.FullName)"
          Write-Host "Manifest: $($releases.FullName)"
      - name: Upload Windows installer artifact
        uses: actions/upload-artifact@v4
        with:
          name: FORTISSIMO-Windows-Phase7
          path: |
            desktop/out/make/**/FORTISSIMO-Setup.exe
            desktop/out/make/**/*.nupkg
            desktop/out/make/**/RELEASES
          if-no-files-found: error
          retention-days: 14
